#include<stdio.h>
int main()
{
  int Num, i, Sum = 0;
  scanf("%d", &Number);
  
  for(i = 1; i <= Num; i++)
  {
     Sum = Sum + i;
  }
  
  printf("Sum of Natural Numbers = %d", Sum);
  return 0;
}
